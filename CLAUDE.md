# zhuzong · 项目入口

> Franklin 的**商务线**：给客户出合作方案 / 报价，跨所有客户。建于 2026-09-06。本目录是 git 仓库，收工时提交一次。
> ⛔ 这里是 Franklin 卖自己服务的报价，不是任何客户商品的售价。⛔ 松石公主的内容、知识、代码不在这里，在 `songshi-princess` 仓库。

## 每次开工先做

1. 读 `方案工具/怎么出方案.md`（定价逻辑、方案结构、发出前检查清单）
2. 看 `台账.md`：哪些方案在等客户回复、哪些有待定数字
3. 接手某个客户：读 `客户/<客户>/01-需求/需求摘要.md` 和 `02-交流纪要/` 最新一篇

## 目录

| 路径 | 是什么 |
|---|---|
| `方案工具/怎么出方案.md` | ⭐ 核心知识：Franklin 怎么定价、方案长什么样、发出前查什么 |
| `方案工具/定价锚与条款库.json` | 历史上真实报出去的数字（带出处）+ 反复用的条款句子。出新方案时参照，不自动算 |
| `方案工具/proposal.py` | 方案 JSON → .md + .html；`new / build / list / ledger` |
| `方案工具/模板/` | 两选项.json（开发类）、清单.json（服务类）、交流纪要.md |
| `客户/<客户>/01-需求/` | 对方发来的需求文档原文 + 我方「需求摘要」 |
| `客户/<客户>/02-交流纪要/` | 每次沟通一个文件，没录音就靠它 |
| `客户/<客户>/03-方案/` | `<客户>-合作方案-vN.json` 和生成的 .md/.html |
| `台账.md` | `proposal.py ledger --write` 生成的全部方案一览 |

## 怎么用

```bash
python3 方案工具/proposal.py new 某客户 --项目 某项目 --模板 两选项   # 或 --模板 清单
#   → 把需求文档放进 客户/某客户/01-需求/，聊完填 02-交流纪要/，改 03-方案/某客户-合作方案-v1.json
python3 方案工具/proposal.py build 客户/某客户/03-方案/某客户-合作方案-v1.json
python3 方案工具/proposal.py build --all && python3 方案工具/proposal.py ledger --write
```

HTML 用浏览器打开 → 打印 → 存 PDF，就是发给客户的版本。

## 规矩

1. ⛔ **方案发给客户前，Franklin 亲口说「发」**。agent 只出稿。
2. **已发出的版本不改**，要改出新版本号（v1 → v2）。JSON 顶部 `_说明` 写清是哪天发出的。
3. 数字来源写在 `定价锚与条款库.json`，新报出去的价格回填进去（带客户和日期）。
4. **每次交流后 3 分钟内写纪要**，倒推出来的内容要标「倒推」。
5. 发出前过一遍 `怎么出方案.md` 第四节的检查清单，尤其是**范围边界**和**留空数字**。
6. `台账.md` 和 .md/.html 是生成物，⛔ 别手改。

## 收工

- `python3 方案工具/proposal.py build --all && python3 方案工具/proposal.py ledger --write`
- 有新的定价或条款 → 回填 `定价锚与条款库.json`；有新经验 → 改 `怎么出方案.md`
- `git add -A && git commit`
